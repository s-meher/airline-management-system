services:
  db:
    image: postgres:16
    container_name: flightdesk_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: flightdesk
    ports:
      - "5432:5432"
    volumes:
      - flightdesk_pgdata:/var/lib/postgresql/data

volumes:
  flightdesk_pgdata:

